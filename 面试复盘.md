# 项目面试复盘：LangGraph 可定制性格智能体

## 一、简历项目描述（可直接放简历）

**基于 LangGraph 的多智能体个性化对话系统**（个人项目）
- 架构：FastAPI + LangGraph 后端 + 原生 JS 前端，OpenAI 兼容协议接入 DeepSeek 等大模型
- 实现 3 节点状态图：生成节点（大模型+工具调用）→ 评审节点（独立 Critic 质检）→ 条件路由（PASS 输出 / REVISE 打回重写，上限 2 轮）
- 工具系统：计算器、课程知识库 RAG 检索、时间查询，模型通过 tool_calls 自主决策调用
- RAG：基于字二元组倒排索引的本地轻量检索，支持 txt/md/pdf 课件自动切块入库，零外部向量服务依赖
- 个性化：前端可配置人设（性格描述/语气/三维性格滑杆），作为 system prompt 动态注入

## 二、面试高频问答

**Q1：为什么选择 LangGraph，而不是直接调大模型 API？**
A：直接调 API 只有"一问一答"，没有状态管理和流程控制。LangGraph 用状态图把"生成→评审→重写"固化为可路由的流程：评审节点独立成节点，配合条件边实现自动打回；state 里的 revision 字段做循环保护。这样的流程用 prompt 工程很难稳定做到。

**Q2：LangGraph 最大的坑是什么？**
A：state 的 reducer 机制。消息列表必须声明 `Annotated[List, operator.add]`，否则节点返回的消息会"覆盖"而不是"追加"历史——我们的工具消息就把对话历史整个覆盖掉了，DeepSeek 收到一条没有 tool_calls 前置的孤立 tool 消息，直接 400。这个 bug 的排查过程就是理解 LangGraph 状态合并机制的最好教材。

**Q3：工具调用（Tool Calling）的完整流程？**
A：llm.bind_tools 把工具 schema 注入模型 → 模型返回 AIMessage 带 tool_calls 字段（含工具名和参数 JSON）→ 路由函数检测 tool_calls，转入 ToolNode → ToolNode 执行对应函数，结果封装为 ToolMessage 追加进 state → 回到生成节点，模型基于工具结果生成自然语言回复 → 无 tool_calls 则路由到评审节点。

**Q4：RAG 是怎么实现的？为什么不用向量数据库？**
A：启动时扫描 knowledge 文件夹，按 200 字符滑窗切块；检索用查询与块的字二元组交集打分，返回 Top-2 拼进工具结果。没用向量库是因为：项目部署要零依赖、数据量小（课件级）、可离线运行。承认局限：语义检索能力弱，升级路径是换 embedding 模型 + FAISS/Chroma，接口不变（rag_retrieve 函数内部替换实现）。

**Q5：多智能体体现在哪？**
A：评审节点 reflector_node 是独立的第二个"智能体"，有自己的 system prompt（评审标准），与生成节点解耦。它只输出 PASS/REVISE 判断，不代写内容——生成与评审职责分离。这是多智能体协作的最简形态，再进一步可以拆成"检索 Agent + 总结 Agent + 校对 Agent"。

**Q6：前后端怎么通信？遇到过跨域问题吗？**
A：前端 fetch POST /chat，携带消息、人设配置和最近 20 条历史；后端无状态，人设每次请求都重新注入 system prompt。CORS 通过 FastAPI 的 CORSMiddleware 放开。历史存在浏览器 localStorage，服务端不落库——优点是隐私好、部署简单，缺点是不能跨设备同步。

**Q7：如果用户量变大，这个架构怎么改？**
A：① uvicorn 换 gunicorn 多 worker；② LLM 调用改异步 + 流式输出（SSE），现在 invoke 是阻塞式整段返回；③ 历史改存 Redis/数据库；④ RAG 升级向量索引 + 缓存热点查询。

**Q8：怎么评估这个智能体的效果？**
A：功能性：工具调用的准确率（该调时调、不调时不调）、RAG 检索命中率。生成质量：评审节点的 PASS 率可以作为自动指标；人工抽检人设一致性（语气是否匹配滑杆参数）。还有端到端满意度：用户重问率（答非所问会导致用户换说法重问）。

**Q9：人设个性化是怎么做到的？参数真的有效吗？**
A：前端把性格描述、语气风格、三个量化参数（幽默/热情/话多 0-100）拼进 system prompt， temperature 0.8 保留发挥空间。量化的三个参数主要是给模型的显式约束，效果上"话多"对回复长度影响最明显，语气影响用词风格。局限性：本质是 prompt 工程，模型权重没变，强人设下长对话可能出现"人设漂移"。

**Q10：这个项目下一步做什么？**
A：① RAG 升级语义检索；② 加长期记忆（把用户偏好写进向量库，跨会话生效）；③ 流式输出改善体感延迟；④ 评测集：整理 50 条通信原理问题做自动化回归测试。

## 三、这个项目教会你的核心概念（自查清单）
- [x] LangGraph StateGraph：节点、边、条件路由、START/END
- [x] 状态 reducer（Annotated + operator.add）
- [x] Tool calling 全链路（bind_tools → tool_calls → ToolMessage）
- [x] 多智能体职责分离（生成 vs 评审）
- [x] RAG 检索增强（切块、索引、Top-K）
- [x] system prompt 工程（人设注入）
- [x] 前后端分离、CORS、无状态 API 设计
